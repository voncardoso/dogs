import Feed from './Feed/Feed';
import Loading from './Help/Loading'
const Home = () =>{
    return(
        <section className='container mainContainer'>
           <Feed/>
        </section>
    )
}

export default Home;